Alpha stage

Based off [[https://github.com/DISTRHO/imgui-template-plugin]]

Building:

toolchain file must be specified eg. for linux build:

cmake -B build -DCMAKE_TOOLCHAIN_FILE=default-toolchain.cmake ./
cmake --build build

building on windows/mac not supported
